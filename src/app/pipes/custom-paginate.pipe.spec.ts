import { CustomPaginatePipe } from './custom-paginate.pipe';

describe('CustomPaginatePipe', () => {
  it('create an instance', () => {
    const pipe = new CustomPaginatePipe();
    expect(pipe).toBeTruthy();
  });
});
